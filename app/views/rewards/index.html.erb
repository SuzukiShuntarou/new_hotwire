<% @rewards.each do |reward| %>
  作成者：<%= reward.user.email %>
  <br />
  <%= reward.completiondate %>に　
  <%= reward.location %>で　
  <%= reward.content %>する
  <br />
  <br />
<% end %>

<%= paginate @rewards %>
