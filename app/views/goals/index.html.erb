<% @goals.each do |goal| %>
  作成者：<%= goal.user.email %>
  <br />
  id：<%= goal.id %>
  目標：<%= goal.content %>
  進捗率：<%= goal.progress %>
  <%= link_to "編集", edit_goal_path(goal) %>
  <br />
  <br />
<% end %>

<%= paginate @goals %>

<%= link_to "新規作成", new_goal_path %>
